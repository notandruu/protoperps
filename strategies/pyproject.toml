[project]
name = "trading-strategies"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiokafka>=0.11",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
